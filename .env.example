# Server
PORT=8080

# Firebase JWT (web app path)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_SIGN_IN_PROVIDER=your-firebase-sign-in-provider

# Slack (events path)
SLACK_SIGNING_SECRET=your-slack-signing-secret
